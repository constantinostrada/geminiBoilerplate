# geminiBoiler

A basic HTML, CSS, and JavaScript boilerplate.

## Setup

1.  Open `index.html` in your browser.

## Scripts

*   `start`: Opens `index.html` in your browser.
*   `lint`: Runs ESLint on the project.
*   `format`: Runs Prettier to format the code.
